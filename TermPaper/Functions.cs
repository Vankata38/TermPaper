namespace TermPaper;

public class Functions
{
    // Example input: DEFINE func1(a, b): "a & b"
    // Example input: DEFINE func2(a, b, c): "func1(a, b) | c"
    // The job of the Define function is to parse the input and store the function in a HashMap data structure, where the funcX is the key and the Tree is the value.
    static void Define()
    {
        
    }
    
    static void Solve()
    {
        
    }
    
    static void All()
    {
        
    }
    
    static void Find()
    {
        
    }
}